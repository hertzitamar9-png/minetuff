name: build

on:
  push:
    branches: [main]
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
      - name: Compile, test and verify
        run: mvn -B -ntp verify
      - name: Verify PowerShell launchers
        shell: pwsh
        run: ./scripts/verify-scripts.ps1
      - name: Upload plugin jar
        uses: actions/upload-artifact@v4
        with:
          name: MineTuff-plugin
          path: target/minetuff-mines-*.jar
          if-no-files-found: error
